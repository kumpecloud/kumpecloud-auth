# Disabled: KumpeCloud Auth does not publish npm packages from this repository.
name: Changesets

on:
  workflow_dispatch:

jobs:
  changesets:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Changesets publishing is disabled for KumpeCloud Auth."
