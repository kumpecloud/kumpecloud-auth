# Disabled: upstream Logto Cloud dispatch is not used by KumpeCloud Auth.
name: Repository Dispatch

on:
  workflow_dispatch:

jobs:
  event_dispatch:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Repository dispatch is disabled for KumpeCloud Auth."
